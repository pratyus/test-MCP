# Server Configuration
PORT=3000

# SailPoint API Configuration (Mock values for now)
# In a real deployment, these would be actual credentials and URLs for your SailPoint tenant.
SAILPOINT_TENANT_URL=https://mock-tenant.api.identitynow.com
SAILPOINT_CLIENT_ID=mockClientId
SAILPOINT_CLIENT_SECRET=mockClientSecret
SAILPOINT_CONTRACTOR_SOURCE_ID=mockContractorSourceId # Example Source ID for contractor creation
SAILPOINT_TERMINATED_LCS=terminated # Example Lifecycle State for terminated contractors

# MCP Server JWT Secret (CHANGE THIS IN A REAL DEPLOYMENT!)
MCP_JWT_SECRET="a-secure-random-string-for-mcp-jwt-secret-dev-only"

# Optional: For testing or specific environments
NODE_ENV=development
